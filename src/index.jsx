// Importe as bibliotecas básicas do React
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App"

const root = document.getElementById('root');

const react = ReactDOM.createRoot(root);

react.render(<App />)